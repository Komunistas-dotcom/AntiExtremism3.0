"""Графический интерфейс: мастер «выбор источников → проверка → отчёт».

Построен на Tkinter — он входит в стандартную поставку Python на Windows,
поэтому не добавляет ни одной новой зависимости и не усложняет сборку
в `.exe` (см. CLAUDE.md §0, §5).
"""

__version__ = "0.1.0"
