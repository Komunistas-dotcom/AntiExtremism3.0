# Собираемый снапшот реестра — создаётся из исходного файла, в репозитории не нужен
*.db
*.db-journal

__pycache__/
*.py[cod]
.venv/
venv/

# Данные пользователя не должны попадать в репозиторий ни при каких условиях
exports/
observations/
*.report.json
